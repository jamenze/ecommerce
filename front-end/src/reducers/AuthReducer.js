// A reducer is a FUNCTION that returns a piece of state

// initialize state as an empty array
// for now the AuthReducer is a function that returns an empty array
export default function(state = [],action){
	return state
}